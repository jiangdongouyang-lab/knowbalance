---
source_id: K018
title: 成绩统计器综合项目
module: Python基础
difficulty: integrated
prerequisites: [K007, K009, K013]
keywords: [成绩统计, 综合项目, 循环, 列表, 函数, 平均分]
---

# K018 成绩统计器综合项目

## 1. 核心定义

成绩统计项目可综合练习列表、循环和函数。

## 2. 必须掌握

- 成绩统计项目可综合练习列表、循环和函数。
- 成绩列表可以用 for 循环逐项累计求和。
- 把统计逻辑封装为函数可提升复用性。

## 3. 示例

### 成绩统计器最小项目

```python
def average(scores):
    return sum(scores) / len(scores)

scores = [80, 90, 75]
print(max(scores), min(scores), average(scores))
```

成绩统计项目把列表、循环/聚合和函数组合成完整任务。

## 4. 常见错误

- 把概念记成语法碎片，不结合实际任务使用。
- 生成内容时不标注 source_id 与 fact_id，导致后续无法审核。

## 5. 实操任务

- 实现成绩平均分函数
- 输出最高分、最低分和平均分

## 6. 分阶测试题

- Level 1: 成绩统计器综合项目通常至少组合哪些知识？
- Level 2: 请完成一个与“成绩统计器综合项目”相关的小练习。

## 7. 可引用事实

- F001: 成绩统计项目可综合练习列表、循环和函数。
- F002: 成绩列表可以用 for 循环逐项累计求和。
- F003: 把统计逻辑封装为函数可提升复用性。
