# PY043 文本词频统计项目

- 模块：Python程序设计
- 难度：intermediate

## 可审核事实
- PY043:F001 — 词频统计用于计算词语在文本中出现的次数。
- PY043:F002 — 字典适合保存词语到次数的映射。
- PY043:F003 — 文本词频统计通常包含分词、计数和排序三个步骤。

## 教学示例

```python
words = ["Python", "å­¦ä¹ ", "Python"]
freq = {}
for word in words:
    freq[word] = freq.get(word, 0) + 1
print(freq)
```

ç¨å­å¸ç´¯è®¡æ¯ä¸ªè¯è¯­åºç°æ¬¡æ°ï¼æ¯è¯é¢ç»è®¡çæ ¸å¿ã

## 练习任务
- 完成一个与文本词频统计项目相关的可评分练习
- 说明文本词频统计项目中的关键步骤

## 题目种子
- `choice`：文本词频统计项目最需要保留什么以便审核？（答案：source_id 和 fact_id；引用：PY043:F003）
- `short_answer`：说明文本词频统计项目的核心作用。（答案：词频统计用于计算词语在文本中出现的次数。；引用：PY043:F001）
- `debugging`：判断正误：文本词频统计项目可以脱离规则和证据随意生成。（答案：错误。应依据明确规则和可追溯事实组织。；引用：PY043:F003）
- `practice`：围绕文本词频统计项目完成一个小任务。（答案：能体现：词频统计用于计算词语在文本中出现的次数。；引用：PY043:F001）
