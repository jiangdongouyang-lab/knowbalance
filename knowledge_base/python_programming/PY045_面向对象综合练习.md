# PY045 面向对象综合练习

- 模块：Python程序设计
- 难度：intermediate

## 可审核事实
- PY045:F001 — 面向对象综合练习通常先识别对象、属性和方法。
- PY045:F002 — 对象列表可保存多个同类实例。
- PY045:F003 — 方法调用可让每个对象执行自己的行为。

## 教学示例

```python
class Student:
    def __init__(self, name, score):
        self.name = name
        self.score = score
    def passed(self):
        return self.score >= 60
students = [Student("å°æ", 92), Student("å°çº¢", 55)]
print([s.name for s in students if s.passed()])
```

Student ç±»å°è£å§åãæç»©åæ¯å¦åæ ¼çè¡ä¸ºã

## 练习任务
- 完成一个与面向对象综合练习相关的可评分练习
- 说明面向对象综合练习中的关键步骤

## 题目种子
- `choice`：面向对象综合练习最需要保留什么以便审核？（答案：source_id 和 fact_id；引用：PY045:F003）
- `short_answer`：说明面向对象综合练习的核心作用。（答案：面向对象综合练习通常先识别对象、属性和方法。；引用：PY045:F001）
- `debugging`：判断正误：面向对象综合练习可以脱离规则和证据随意生成。（答案：错误。应依据明确规则和可追溯事实组织。；引用：PY045:F003）
- `practice`：围绕面向对象综合练习完成一个小任务。（答案：能体现：面向对象综合练习通常先识别对象、属性和方法。；引用：PY045:F001）
